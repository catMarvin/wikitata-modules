/**
 * Components barrel.
 *
 * All components ship headless: they consume `Composition` / URL strings as
 * props and render. Token / palette application is the consumer's job (apply
 * Steel-blue tokens per wikitata card 68c40443).
 */

export { default as Composition } from './Composition.js';
export { default as HeroVideo } from './HeroVideo.js';
